## Problem

<!-- What is wrong or missing, and which issue owns it (link it). -->

## Scope

<!-- What this PR changes and what it deliberately leaves out. -->

## Validation

<!-- Exact checks run and their results: `mise run verify` (or `python3 scripts/verify-docs.py`,
     `git diff --check`), issue-specific checks. Do not claim a check passed unless it ran. -->

## Limitations

<!-- Evidence gaps, deferrals, anything the reviewer must know before assessing. -->
